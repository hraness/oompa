# Oompa product website and documentation

Domain update, 2026-09-10: [the current domain plan](./oompa-app-domain.md) supersedes earlier host choices in this record. Current website and app destinations are `oompa.app` and `app.oompa.app`; prior hosts grant no current routing or authentication authority. Dated delivery evidence remains historical.

Status: implemented; final integration and delivery pending. Owner: Hraness. Source checked: 2026-09-08. Reassess: 2026-10-20.

## Scope and decision

Present Oompa as the web workspace and local CLI it now is. Replace the homepage's complete reference dump with a bounded product explanation and real UI examples. Split public documentation by reader task, retain canonical detailed contracts, and make the README an entry point rather than a second manual.

The user explicitly requested Sponge as a reference and actual Direct-powered UI mockups. Reference: Sponge main `9c1ea788fa6dea9b312799dc4b6194b9606e1d72`. The useful pattern is a static, separately built fixture app using production components, not the authenticated app or an invented dashboard. Oompa retains its own Bun, Vite, StyleX, CSP, and deployment architecture.

No provider, account authority, routing, operational activation, or release-version changes are in scope. The admitted v0.7.0 artifact and the current daemon/hosted-writer capacity gate remain separate facts. This work does not grant rollout permission or re-publish that immutable artifact. Shared semantic-theme work remains independently owned.

## Content admission

The six authored per-page admission records live in `site/docs-content.ts`. Each names its reader job, original Oompa-specific contribution, overlap decision, checked source, owner, reassessment date, and six scores. These are agent-authored records, not a human editorial approval. Independent agent source/claim-risk review accepted all six after correcting the Claude first-run command and putting the current setup notice before the web guide's local-effect commands.

Homepage decision: revise. Reader job: understand what Oompa does and choose a next step. Original contribution: actual Oompa screens connect local execution, session supervision, account separation, and browser use. Canonical host: oompa.dev owns the product. Nearest URLs: `/docs/` routes readers, `/docs/start/` gives a procedure, `/docs/web/` explains browser tasks. The homepage introduces the product without duplicating those jobs. Scores: utility 2, evidence 2, confidence 2, host fit 2, voice 2, maintenance 1. Human voice is limited to the existing supplied maker biography; no new experience or endorsement is invented.

Privacy decision: keep its canonical `/privacy/` route and full policy; correct heading hierarchy and shared navigation only. README decision: revise to shared product claims, exact installation, adjacent startup restriction, and canonical guide links. Legacy homepage fragments redirect to their owning detailed sections with a native link fallback. Detailed reference remains available without JavaScript and in canonical Markdown exports.

## Implementation boundaries

- Main owner: public content, homepage, docs shell, parent preview controls, README, integration and browser verification.
- Docs worker: authored guide registry, admission records, focused contracts, identified runbook drift.
- Preview worker: `app/fixtures/product/`, fixed fictional observations, actual app reducers/components, IO-only aliases, and bounded import-policy exceptions.
- Preview builder: private compiler completion and closed public projection at `/examples/app/`.
- Build worker: exact static routes, Markdown publication, analytics taxonomy, strict compilation contracts, and production headers.

Direct is a pinned development dependency and is absent from production application and CLI entry graphs. Public previews are inert, use fictional accounts, refuse all commands and persistence, and have no provider credentials or live connection. A script-only iframe sandbox creates an opaque origin. Parent messages must match the exact frame window, expected view, and opaque origin. Offline CSP, a closed selector, Direct's IO firewall, stable rendering probes, and compiled-output checks are separate safeguards. Preview evidence proves rendering, never real sign-in or provider qualification.

## Acceptance

1. First-time readers can identify the web workspace, local execution, provider support, and next step without reading the reference.
2. Six docs routes have distinct jobs, exact commands, canonical Markdown, bounded local search, active navigation, valid headings and links, and current availability wording.
3. Four actual app screens render on light/dark marketing pages and in narrow/wide layouts, with clear fictional disclosure, zero forbidden IO, visible failure handling, and keyboard-operable scene and enlargement controls. The app itself currently has a dark-only theme; previews preserve that real appearance rather than inventing a fixture-only theme.
4. Existing installation, recovery, privacy, provider, and authority assertions move to the appropriate guide. None is removed merely because the homepage becomes shorter.
5. Strict StyleX completion, approved fonts, no inline presentation, immutable artifacts, and private renderer/proof separation remain intact.
6. Focused tests, independent source/editorial review, repository final gate, browser acceptance, and deployment verification pass on their applicable exact trees.

## Evidence

Research and source audit used Oompa `3da9744acad51faf898f4d35c78bbe5f5be5dd2e` and the Sponge revision above. The later current-main integration incorporates `5027d21d8e4a128f87f2a78810b3f23929404061`, the separately reviewed attention-key installer from PR #154, without conflicts or operational activation. Parallel semantic-theme PR #158 remains independently owned; no theme source or dependency pin was copied into this change.

Independent review covered public claims and source behavior separately. The docs reviewer accepted the six distinct reader jobs, first-party authority, individual admission records, and bounded maintenance dates. The implementation reviewer accepted the isolated build projection, exact CSP/CORS separation, docs routing, and browser-driver integration. A second source pass accepted the independent enlarged-frame loading lifecycle. The integration owner separately reviewed the worker-owned fixtures, parsed observations, real reducers, IO-only aliases, and private-to-public compiler projection. These are agent reviews, not human approval or live-provider evidence.

Focused evidence before the final gate:

- Full site-build regression suite: 20 tests, 986 assertions, passing.
- Compiler-route and analytics suite: 15 tests, 224 assertions, passing.
- Focused CSP and parent-bundle checks: 2 tests, 37 assertions, passing.
- Docs, parent preview, loading controller, and marketing tests: 22 tests, 802 assertions, passing before the subsequent modal-isolation case and final copy cleanup. The final aggregate must include those additions.
- Compiled-CSS regressions first reproduced missing border declarations, then passed with explicit physical longhands: 2 tests, 118 assertions. Focused ESLint passed for the style repairs and integration changes.
- Final public-content contract rerun after copy and anchor repairs: 48 tests, 1,736 assertions, passing. An independent agent accepted the simpler rollout wording and the Settings active-hours correction against their unchanged source contracts.
- Real site builds passed. The first failed build exposed a forbidden `noscript` element; the fallback now uses ordinary static text hidden only after enhancement initializes. Compiler policy was not weakened.
- The first typecheck exposed one remaining property-test union mismatch. Its boolean equality repair preserves the assertion. A subsequent complete-gate attempt on `783e277cc70f15047999074f71c40980a522ad41` passed installer, architecture, security, lint and type checks; its script phase passed 1,481 tests, skipped four existing cases and failed one stale release-documentation assertion. That failure remains evidence, not final acceptance; the integrated tree requires a fresh complete gate.
- The release-documentation test reproduced the obsolete README assertion, then passed with 78 assertions after following the README and homepage links to their canonical availability guide. Byte-exact installation, release-bound intent recovery, diagnostic-only uncertain-tag handling, and explicit capacity restrictions remain covered. Focused lint passed, and independent integration review accepted the migration without content or authority changes.

The current-main join has no overlapping paths with the website change. Independent review confirmed that its eight files do not change public CLI grammar, website/app inputs, or any file in the reviewed package inventory. It adds a separately invoked installer and operator tests, not activation. The merged tree still requires the unchanged complete gate.

Interactive browser review used the built site with production-equivalent CSP/CORS, at desktop and a 390-by-844 mobile viewport. The actual grid, conversation, and closed-choice question rendered. Guide search, Escape clearing, native modal focus, Escape closure, and focus restoration worked. Mobile docs had no horizontal overflow. The deterministic 1200-by-630 social image was visually inspected. These observations are not final browser acceptance.

That browser pass found and prompted three repairs: bind the browser timer cancellation correctly, preserve separate enlarged-frame readiness and timeout, and replace silently omitted border shorthands with tested compiler-supported longhands. The product anchor also gains sticky-header clearance, and the JavaScript fallback notice disappears when controls become available. A queued, never-admitted browser run was canceled by its owner so the repaired tree could converge; no admitted work or gate was bypassed.

The task changes the public site, docs discovery, static build and test graphs, and development-only preview dependency. The sole production-app edit is factual active-hours help text. Provider execution, account credentials, Effect services, routing, authority policies, runtime dependencies, immutable release version, and deployment identities are unchanged.

Independent script-disabled current and exact-base package archives preserve all 167 files, nine directories, paths, types, and modes. Only README, package-manifest, and third-party-notice bytes differ. Their sizes change from 107,822 to 7,455, 6,845 to 6,882, and 2,041 to 2,345 respectively. The updated canonical metadata inventory is 176 rows, 8,316 JSON bytes, SHA-256 `ac54ae5ba1f8f5d9a49fa20a63c171ea0b2e263e024cdb04136c1bd6c8e2871b`. All other 164 archive file hashes match the base, and every final archive file matches its current source. The strict inventory check accepts the new archive and rejects both preceding inventories; package-policy tests pass with 11 tests and 42 assertions. No release artifact was published or replaced.

The preview builder includes Direct's complete pinned MIT license through a plugin banner and preserves inline legal comments during minification, with a build assertion that its exact text survives into the public JavaScript. Failed assertions reproduced source-comment and default minifier stripping; attribution now joins the compiled graph before hashing and sealing without overriding compiler-owned output configuration. Independent source review accepted the attribution and development-only boundary. The real compiled-artifact regression then passed with one test and 744 assertions, including the exact public license text.

The complete local gate on `23806b66a6458b1e5d075368ae4ef25b2bcfa729` passed the installer, architecture, security, lint, type, script, plugin, core, Convex, site and app phases, along with every production build. The observed counts include 1,543 script tests with four existing skips, 108 local-plugin tests, 29 cloud-plugin tests, 321 Convex tests, 117 site tests and 549 app tests. The final package phase then rejected the public Direct import as an unreviewed package scope. This remains a failed aggregate, not acceptance. The repair admits only the exact public `@hraness/direct` package, preserving private-sibling, misleading-suffix, credential and path rejection. Its regression reproduced the failure before the change; the full policy file then passed 25 tests and 232 assertions.

The next normal main join incorporates `4d4447a99bb08b3469e620f7f179b9a7d3e8bf62`, including its reviewed delivery policy and browser hardlink admission repair. Under the current `CONTRIBUTING.md`, complete required CI owns final source aggregation after independent impact and command-equivalence review. The unchanged workflow-contract tests pass locally with 28 tests and 944 assertions, including whole-file sharding, failure propagation and exact expanded command coverage. The installed local-efficiency 0.4.1 repository-adoption check reports the managed guidance current. Independent inventory review confirms that none of the 13 incoming main paths ship in the reviewed package. This policy does not waive the observed package failure, local native acceptance, installation checks or production proof.

The final reader-journey audit also repaired one first-run gap: the web guide now links directly to the canonical protected JSON sign-in formats and puts machine sign-in before browser enrollment. The new regression failed against the preceding copy, then the complete docs-content file passed nine tests and 471 assertions. Independent integration review checked the wording and target against the retained sign-in contract; README and package bytes are unchanged.

Draft PR #163 publishes the reviewed implementation. Its first CI run, `34307859969` attempt 1, passed all six source shards and all remainder phases before package admission. Both operating systems rejected a literal synthetic negative-test package name in the preview-builder test. Constructing that name preserves the negative case without allowing a package or scope; its six tests and 54 assertions pass. Historical admission uses the existing exact-commit, complete-patch-digest, single-quoted-fixture mechanism, not a new scanner exemption. The source introduction and removal patches were independently checked; sensitive-text scans remain untouched.

The same CI run rendered all nine public routes and all four genuine Direct states, then failed the final browser isolation assertion. Its retained receipt identifies the pinned Playwright service-worker blocker reading a getter forbidden by the opaque iframe sandbox. The reviewed verifier repair instead seals the native registration method without touching that getter, retains every application console/runtime error as a failure, and requires a separate native negative control to prove zero requests, registrations or workers. The sentinel script must be absent from the served inventory. Unit evidence passes with 38 tests and 455 assertions; native acceptance remains pending. The failed run positively collected nine owned browser processes with zero survivors.

Final manual review confirmed the real grid, Settings and phone-sized question screens, plus readable mobile documentation without horizontal overflow. It also found that the document reset removed the native dialog's centering margins. Four explicit compiler-supported automatic margins repair the position; the compiled-CSS regression first failed, then all three tests passed with 122 assertions. The owned preview server was stopped successfully and the browser viewport restored. The repaired geometry still needs fresh visual readback.

The complete local `bun run check:package` passed on `7c0e260459890210bf4acb3ab6ebbe4062352fa9`, tree `430664c90ca977d9d9e83eab298df3ba5bb74b0f`, in the isolated complete-history checkout. It proved the public source and history scans, exact archive inventory, isolated local and global consumers, restored PTY shell and global daemon lifecycle. No release was published. Independent review also accepted the getter-free verifier's isolation and native negative-control design.

CI run `34308727105` attempt 1 proved that native service-worker control with zero requests, registrations and workers. Its next failure identified a verifier lifecycle race: native dialog visibility changes before the queued close event removes its iframe. The follow-up waits for actual detachment within the existing locator and profile deadlines, then retains the original zero-frame and focus-return assertions. A deterministic queued-cleanup regression reproduced the failure; the three focused cases then passed with eight assertions, including timeout propagation and refusal of a surviving child. No product lifecycle, timing budget or error policy changed. The owner canceled the still-waiting local browser wrapper before admission so it could validate the converged repair.

Local native acceptance on `200b686` passed the dialog cleanup and reached the session guide, then failed a native response-body read after its document lifetime. CI run `34309675840` separately reached the existing profile deadline while collecting homepage resource bytes. Both runs retained failed receipts and proved zero surviving owned browser processes. The follow-up captures parent resource bodies immediately, settles every retained native body before a scene change or modal removal, and exercises each guide's lazy default example before leaving its document. Exact byte comparisons, response/request census, sandbox and error policy remain required, with no timeout increase or retry. The focused browser-driver file passed 44 tests and 472 assertions; lint and diff checks passed. Native proof of this repair remains pending.

Native acceptance on `1d8abc4` then completed those reads and exposed five child requests but only four responses on the session guide. Progressive enhancement was unnecessarily reassigning the server-rendered iframe URL, which can abort its first navigation. An actual bundled-entry regression observed the duplicate assignment before repair. The initializer now preserves the exact server URL; across all four scenes the regression requires no initial or same-selection navigation and exactly one navigation for a changed selection. All five product-preview tests passed with 238 assertions, with lint and diff checks passing. The tighter browser census stays in place; native proof of the product repair remains pending.

Independent review also identified the complementary cached-child ordering: readiness may precede a delayed parent listener. A single scene-bound status request now replays an already-settled public observation without navigation, execution or state reset. The child accepts only its exact parent window and served origin; malformed or cross-scene requests are ignored and failure remains sticky. The bundled-parent test delivers cached readiness immediately after listener registration and proves zero initial navigation. Ten preview tests passed with 284 assertions; seven fixture contracts passed with 208 assertions. The shared protocol file is now an exact browser source-census input; ten handoff tests passed with 1,663 assertions, including a red-then-green proof that same-length mutation invalidates the original browser request. Independent review accepted the handshake and the integration owner reviewed the census change. Scoped lint and diff checks passed.

The protocol also has bounded fixed-seed property tests for arbitrary input envelopes and generated publish/replay sequences. They preserve exact fields, accessor refusal, scene binding, latest-status replay and sticky failure. The complete preview file now passes 12 tests and 2,445 assertions; scoped lint and diff checks pass.

An unrelated Ubuntu source-shard failure in run `34310372001` was traced read-only to the existing installer's test sentinel: it checks file existence before reading a directly written PID payload, so creation may precede complete contents. This task changes neither installer source nor that test. The observation is retained as a failed run, not represented as acceptance; the final candidate still requires all unmodified source shards to pass.

CI run `34311157134` passed complete compiled app/site browser acceptance on the handshake candidate `4b85da0` in 2 minutes 53 seconds. Before local admission, main advanced to `2b876707a5005e6143c14d218e934afaf9d6b24e` with the independently owned read-only daemon default-profile observation. The owner canceled the still-waiting local browser job and normally joined main. Both Settings changes and native browser assertions remain intact; real `toMachineView` produces the truthful unsupported observation for fictional profiles lacking that evidence, with no fixture fabrication. Seven fixture tests still passed with 208 assertions.

The package-inventory conflict was resolved from a fresh actual joined archive, not either old digest. Independent and integration-owner readback agreed on 167 files, nine directories, 176 canonical rows, 8,316 JSON bytes and digest `56ac62812868a7fe7bc74e58a41bae8f720869683c691dea2218265efdeea588`. All 164 nonmetadata archive files match exact new-main source; only README, package metadata and notices differ. A separate fresh base archive could not be created because approval-service capacity rejected that attempt before execution; this is explicitly archive-to-exact-source evidence, not a two-archive comparison. No rejected operation was bypassed. The installed-package and browser gates must both rerun on the joined tree because main changed five shipped runtime files and the PTY verifier.

The candidate `60eb827fe4a065f872795ad32a54d22bb97d1fa6`, tree `37a2c0a15450a270915bfffb87141253cbce8108`, passed the sequential local typecheck, pinned native browser and package acceptance gates. The browser retained 209 evidence rows across six completed profiles with unchanged inputs and all 54 owned processes collected. Fresh desktop and phone visual review proved dialog centering, no horizontal overflow, Escape closure and focus restoration. Complete CI run `34313286886`, attempt 1, passed all eight matrix jobs and compiled browser acceptance; Required job `102346728911` passed. CodeQL's two analyses and security summary also passed after exact URL array membership in a test was expressed as explicit element equality. No scan was suppressed.

Immediately before merge, main advanced to `041a06a16015ff8b270a413b8c10ab9ca89e4ca2` with the independently owned shared-theme implementation. The normal merge retains its immutable design-kit 0.6.2 pin, actual menu and semantic recipes, preference bootstrap on navigable pages, and privacy disclosure. The task still owns the concise UI-first homepage, six guides and four real-screen examples. Its closed example boundary must replace only the newly reachable appearance IO, retain the real themed presentation and default palette, and never load the production preference bootstrap or gain persistent storage. The separately compiled example keeps its strict no-bootstrap artifact topology. Parent appearance, preview and analytics bundles stay separate, with Direct barred from parent bundles. The joined browser census and assertions must retain both implementations.

Remaining gates: resolve and independently review the theme join, reproduce the actual joined package inventory, run focused contracts and all required exact-tree local/native/package and CI gates, repeat visual review of the themed result, then merge, guarded website deployment and independent production verification. The preceding passing receipts are historical evidence, not acceptance of this new integration. No task PR merge, release, deployment or production acceptance is claimed. Exact final delivery receipts belong to PR #163 so later external state is not inferred from this pre-delivery source record.

The fresh theme-joined archive has SHA-256 `e938814149d8fa317a89956744cb122967762e9ee1ddec14e75ae09f1f496e75`. Its actual canonical inventory remains 167 files, nine directories, 176 rows and 8,316 JSON bytes; the new inventory digest is `67d370005adcb3267c57939561e4a242eacfdc12680fde5064a4c5e048cda2e4`. All archive files match joined source, and all 164 nonmetadata hashes match exact main `041a06a`. Only README, notices and package metadata differ, with joined sizes of 7,455, 2,396 and 6,882 bytes. This is a fresh joined archive against exact base source, not a repeated or claimed second base archive operation. No package is published.

## Lantern material preparation, 2026-09-12

The website adopts the asset-free portable Lantern material from reviewed design-kit source `eccb0341d8d0ba960a0f02248cf59888062afb0a` without changing the installed component peers. The homepage hero carries the expressive wall. Navigable headers, opaque reading surfaces and the parent preview controls use quiet material paint; actual selected preview buttons and open native disclosures retain matched foreground/background states. The existing palette bootstrap, keyboard focus, disabled states, app examples, install restrictions and product copy remain authoritative.

The canonical five-file snapshot enters the captured foundation after the existing foundations. Static compilation admits the snapshot before and after capture. Browser preparation admits it and binds every source file through the existing custody census. Its MIT license matches the already published shared attribution, so the public artifact inventory remains unchanged: fourteen fonts and two editorial textures, with no material asset URLs. Public-source scanning adds only the exact declaration path inside the complete pinned snapshot and continues scanning all text.

Preparation is not release or production acceptance. Focused source and compiler contracts, independent review, the current-base complete Required CI, and the existing native browser gate remain required. Native material readback must cover light/dark and narrow/wide layouts, real appearance controls, selected preview tabs and disclosures, opaque reading panes, forced colors and reduced transparency. App-only promotion, root website aliases and all operational cutover guards remain outside this source change.
