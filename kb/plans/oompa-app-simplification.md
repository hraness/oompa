---
title: Oompa app simplification and inline conversations
tags:
  - app
  - product
  - docs
---

# Oompa app simplification and inline conversations

## Outcome

The web app becomes a single grid of compact session cards that each hold their own conversation. Starting a session asks for a prompt and a machine, nothing else. The reader never locks or unlocks the tab. The homepage shows one compact meter of remaining provider usage across every account and machine, with the next reset and the current throughput, and the settings page shows the detailed breakdown behind it. Codex sessions start on Astra Ultra, Claude sessions on Fable Max, and start-time routing may downshift a well-defined Codex task to Astra Max. The Codex desktop account switch is removed. The website documentation is self-consistent with the shipped product.

Owner request, 2026-09-10 (paraphrased and ordered):

1. Remove the lock concept entirely: no Lock button, lock screen, idle lock or `Ctrl+L`. Enrollment and device approval stay. The tab unlocks itself when it loads.
2. Starting a session and writing a prompt or answer in a card use multi-line text areas.
3. Remove the conversation screen. The conversation lives inline in the card, compact and minimal in the style of Zed's agent panel: scrollable history of prompts and responses, responses collapsed except the latest, and scrolling that works well for long results.
4. Tighten the card header: less padding around the drag handle and the kebab; the subagent chips have too much vertical padding inside and horizontal padding outside.
5. No account picker and no model picker. A machine picker stays. Always use the best model the connected accounts allow: Astra Ultra on Codex, otherwise Fable Max. Internal routing takes care of token savings automatically.
6. No project picker. Assume the Documents directory on macOS, or its equivalent on Linux.
7. A compact homepage meter of net usage remaining across accounts and machines, including Codex reset credits and when limits free up, and the rate of throughput. Clicking it opens a detailed breakdown in Settings.
8. Remove Codex desktop app subscription switching.
9. Make the documentation self-consistent and current. Example: the overview page's Detailed reference only shows licence information.

## Starting evidence

- `main` at `16969b9` (v0.8.0 candidate; the rename and provider-usage foundation are merged). CI speed-up (#177) and release preparation (#176) are in flight and do not touch these surfaces.
- App survey (2026-09-10): the grid composer is a single-line `Input`; the Lock button sits in the grid header and in Settings; `custody-context.tsx` owns the idle lock and `Ctrl+L`; the session screen is a separate route with a `Sheet` for model, Fast, approvals and provider; `transcript-view.tsx` renders every entry expanded with no virtualization; card header padding and the 2.75 rem drag handle live in `session-card.stylex.ts`; there is no "Reviewer badge" component, the chip the owner saw is a subagent chip whose face is the subagent's role (`subagent-chips.tsx`, 2.75 rem tall).
- Daemon survey (2026-09-10): `session.start` requires an explicit account and resolves the default project when none is named; `oompa init --yes` creates the `Documents` default project; the device registry projects accounts and projects without a default flag; `usage:listAccounts` and `usage:listSnapshots` are `active_device` queries not yet named in the app; the hosted usage projection carries limits with primary and secondary windows, reset times, daily tokens and lifetime tokens, and no reset-credit count or velocity; the routing decision is shadow-only; Codex High and Ultra are bound to contract 1 (Sol) since the 2026-09-06 correction, and contract 2 already binds them to Astra Max and Astra Ultra.

## Constraints and decisions

| Decision | Choice | Why |
| --- | --- | --- |
| Lock | Removed. The account key is unwrapped on load whenever the device is active and bound, and dropped on `pagehide` and on an authority failure. `custody.state` becomes `unlocked`, `unlocking`, `unenrolled`. | Owner request. The key never leaves memory and never survives the page, which was the protection the lock added on top of enrollment. |
| Conversation screen | Removed with its route. `#/session/<id>` resolves to the grid and scrolls the card into view. | Owner request; one screen fewer to keep consistent. |
| Card history depth | A card subscribes to the compact tail (as today) and shows what the tail holds. "Earlier" at the top of the conversation loads the full history for that card only. | A grid of cards must not walk every session's history on mount. |
| Collapsed responses | Every closed assistant message except the newest renders as a one-line summary with a chevron; the newest response and the streaming turn stay open. Thinking stays collapsed. | Owner request: scrolling back through long results must be cheap. |
| Composer modes | The grid composer only starts sessions. Follow-ups are typed in the card. | The selected-session steering mode duplicates the inline composer. |
| Per-session controls | The card menu keeps Archive, Rename, Copy id, ordering, Approvals and Provider switch. Model and Fast controls leave the app; the CLI keeps `session model` and `session fast`. | Owner request to remove the model picker; Fast is a Codex service tier, not a token saving. |
| Start target | Machine picker only. The app picks the machine's first signed-in Codex account, else its first signed-in Claude account (Linux only), and the provider's best preset. | Owner request. Account selection stays explicit in the device command, so the daemon contract does not change. |
| Project | The app sends the machine's default project. Registry version 1 gains an optional `defaultProjectPublicId`; an older registry falls back to the first project. The project picker leaves the app. The CLI keeps `project add`, `project list` and `project use` as an advanced path. | Owner does not use projects; `oompa init --yes` already makes `~/Documents` the default. |
| Codex best model | `high` and `ultra` rebind to contract 2 (Astra Max, Astra Ultra). Established sessions keep their bound contract until reselected, exactly as the 2026-09-06 correction did in the other direction. | Owner request supersedes the Sol default. Contract 2 already exists, so no new contract number is needed. |
| Token-saving routing | Browser start only. A new Codex prompt classified as `well_defined` or `mechanical` may start on `high` (Astra Max) only under exact Astra contract 2 bindings; every other shape and any explicit effort mention keeps `ultra`. Settings → New conversations → Automatic effort is the browser-local off switch. An idle CLI start has no prompt and keeps Ultra. | Owner request. Reuses the existing fenced command without a new daemon writer or prompt contract; never moves a live conversation. This replaces the earlier proposed daemon routing setting. |
| Desktop switching | `src/desktop/*`, the `account.switch` and `account.switch-recover` commands, the daemon port, the installation flag and the website section are removed. The `desktop_switch*` tables and their migrations stay (append-only), as do historical fixtures. | Owner request. Schema history is immutable. |
| Usage meter data | Preserve the legacy v1 daily archive and its byte bounds. A bounded history view may ship separately; live capacity, credits, throughput and both-provider observations require the independently admitted v2 current-head path in the provider-usage plan. | Daily admission is every 24 hours and cannot support a live meter; adding fields breaks older exact-key clients. |
| Docs | Each docs page owns at least one Detailed reference that belongs to its task, or none. `project` moves to the status page. Removed features leave the site, README, release notes and kb plans in the same change that removes the code. | Owner request; `docsPathForSection` throws for an unowned section, so the registry stays exact. |

Unchanged: enrollment and `oompa device approve`, encrypted sync, the device-command authority boundary, the frozen compatibility identifiers listed in `kb/plans/oompa-rename.md`, and the CLI session-start contract.

## Phases

### Phase 1. Custody without a lock

- **Scope:** `app/src/custody/custody-context.tsx`, `app.tsx`, `env.ts`, `custody/idle.ts` and `lock-screen*` (deleted), grid and settings Lock buttons, `site/docs-content.ts` sentence about `Ctrl+L`.
- **Acceptance:** an enrolled and bound tab reaches the grid without a click; the account key is wiped on `pagehide` and on an authority error, which shows the enrollment screen with the error; no `lock` member remains in the custody type; `bun test ./app` passes.
- **Status:** Merged in PR #178 at `ac7273068885ba0747be77910a0acf42420a7092` (2026-09-10). Required CI passed on the reviewed integration candidate.

### Phase 2. Inline conversation cards

- **Scope:** `session-card.tsx` (conversation region, composer, interaction panel, kebab sheet), new `conversation-view.tsx` derived from `transcript-view.tsx` (collapsed responses, "Earlier" loader, auto-follow), `grid-screen.tsx` (start-only composer as a text area, no selection state), `routing/route.ts` (session route becomes a grid anchor), deletion of `session-screen*`, style tightening for the header, drag handle, kebab and subagent chips.
- **Acceptance:** the grid renders every conversation inline; older responses are collapsed and expand in place; the newest response and a streaming turn are open; a card scrolls independently and follows new output unless the reader scrolled up; `Enter` sends and `Shift+Enter` inserts a newline in every text area; the drag handle and kebab fit in a 2 rem row; subagent chips are 1.5 rem tall; `bun test ./app` and the compiled app check pass.
- **Status:** Merged in PR #178 at `ac7273068885ba0747be77910a0acf42420a7092` (2026-09-10). Required CI passed on the reviewed integration candidate.

### Phase 3. Machine-only start

- **Scope:** `model/device-commands.ts` (`sessionStartTargets` becomes per machine with a resolved account and preset), `grid-screen.tsx` picker, `src/cloud/payloads.ts` and `daemon-adapters.ts` (`defaultProjectPublicId` in the registry), `settings-view.ts`.
- **Acceptance:** a machine with a signed-in Codex account starts on that account with `ultra`; a Linux machine with only a Claude account starts on `fable-max`; the payload names the machine's default project, else its first; an old registry without the field still starts; parser and builder tests cover the additive field.
- **Status:** Merged in PR #178 at `ac7273068885ba0747be77910a0acf42420a7092` (2026-09-10). Required CI passed on the reviewed integration candidate.

### Phase 4. Astra binding and start-time routing

- **Scope:** 4a updates `src/domain/presets.ts` active bindings for `high` and `ultra`, tests that pin Sol, source-bound labels and documentation. 4b adds a pure browser-start selector using the conservative task-shape classifier, a finite browser-local preference, an effort hint in the grid composer, and an off switch in Settings. The shadow router, daemon settings, CLI prompt contract and device payload shape stay unchanged.
- **Acceptance:** new and explicitly reselected Codex High and Ultra resolve to `gpt-6-astra`; established contract 1 sessions keep Sol until reselected. A browser start may choose High only for a clearly bounded prompt with exact Astra contract 2 High/Ultra bindings. Unknown input or binding, explicit choices and established sessions never downshift. Turning automatic effort off restores Ultra for every new browser start; malformed or unavailable storage fails off. CLI idle starts keep Ultra because they carry no prompt. One ordinary command records its exact preset and contract, without fabricated historical rule provenance or retry after an uncertain result. Focused policy, storage and cross-layer tests plus compiled browser acceptance pass.
- **Status:** 4a and 4b are implemented in converged PR #182; focused validation and compiled browser acceptance passed, and independent review found no blocker. Fresh Required CI remains pending with the integration owner.

### Phase 5. Remove desktop switching

- **Scope:** `src/desktop/`, `src/domain/desktop-switch.ts`, CLI parser and renderer entries, daemon command kinds and port, `installation.ts`, live-acceptance installation, tests, website section `desktop-account-switching`, FAQ lines, release notes, `docs/live-acceptance.md`, `AGENTS.md` mentions.
- **Acceptance:** `oompa account switch` is an unknown command; `bun run check` passes; the package inventory pin is updated; no public text names the feature.
- **Status:** Implemented in converged PR #182, superseding the source candidate in #179; Required CI remains pending. Focused installer and recovery validation passes (61 tests); the retired CLI actions have explicit rejection coverage. Retained desktop-switch storage history stays intact.

### Phase 6. Usage meter and breakdown

- **Scope:** `app/src/data/functions.ts` and `data/usage.ts` (queries, decrypt, dedupe by account), `model/usage-meter.ts` (historical remaining and scheduled reset; future current-head velocity and runway), `components/usage-meter.tsx` on the grid header, a Settings "Usage" section with per-account windows, reset countdowns and a throughput sparkline; the separately versioned current-head producer, hosted authority, bounded envelope and browser mapping from provider-usage Phase 8, preserving the legacy `src/cloud/usage.ts` contract.
- **Acceptance:** the grid shows, per provider, remaining percent of the tightest window, time to its reset, reset credits when known, and tokens per minute; the meter states surplus or running out from the runway comparison; clicking it opens `#/settings/usage`; an account seen from two machines appears once; a stale or missing snapshot reads as unknown, never as zero; property tests cover the runway arithmetic.
- **Status:** Incomplete. Converged PR #182 contains the repaired read-only legacy Codex daily-history view from #181, with unknown states for stale, expired or unavailable data. The per-provider live meter, credits, throughput, runway and throughput sparkline remain pending the Phase 8 current-head integration in `provider-usage-management.md`. No Convex deployment or cadence change is part of this slice.

### Phase 7. Documentation consistency

- **Scope:** `site/docs-content.ts` pages and reference ownership, `site/content.ts` sections touched by the phases above, README, `docs/beta-release-notes.md`, `docs/usage-management.md`, `docs/model-routing.md`, `kb/plans/provider-usage-management.md` browser-view note.
- **Acceptance:** every docs page's Detailed reference belongs to its task; no page describes the lock, the conversation screen, the account or project picker, the model sheet, or desktop switching; the generated site check and the public-text scan pass.
- **Status:** Source corrections are included in converged PR #182; focused checks and compiled browser acceptance passed, and Required CI remains pending. Browser instructions, reference ownership, preserved moved fragments, app contributor guidance, and candidate notes follow the reviewed implementation, including the daily-history limits and automatic browser effort.

## Delivery

Phases 1 to 3 merged together in #178. Astra binding, automatic effort, desktop removal, documentation and the bounded daily-history view converge in one independently reviewed current-base candidate, with the owner identity on each commit and complete Required CI. The full live usage meter remains a separate incomplete phase; source admission does not clear runtime rollout gates.

## Implementation log

- 2026-09-10: Plan written from the app and daemon surveys. Worktree `hra-worktrees/oompa-redesign`, branch `claude/app-redesign`.
- 2026-09-10: Phases 1 to 3 implemented together. Custody states are `unlocked`, `unlocking`, `unenrolled`; the lock screen, idle timer and shortcut are deleted. `session-card.tsx` holds the conversation (`conversation-panel.tsx` scroller with an "Earlier" loader and anchored scroll; `transcript-view.tsx` folds every closed response except the newest), the composer (`composer-textarea.tsx`, `model/composer.ts`), the interaction panel and a settings sheet limited to approvals and provider. `session-screen.tsx`, `streaming-tail.tsx` and the session route are removed. `sessionStartTargets` is per machine and resolves account, preset and project; the registry projects an optional `defaultProjectPublicId`. Browser acceptance (`scripts/app-browser.ts`) and the product examples render one card for the session views. The React DOM style-boundary hash set gains the new bundle's identifier allocation after a modulo-identifier diff against the reviewed function.

- 2026-09-10: PR #179 installer-pin repair follows removal of desktop composition from the CLI: the documented pin updater binds the normalizer and staged runtime to the changed CLI bytes. Focused installer and recovery suites pass (61 tests), parser coverage passes (73 tests), and CI equivalence passes (33 tests). The unchanged compiled-browser gate passes with 204 evidence rows after the failed CI CSS negative-control sample did not recur locally. Final admission remains the unchanged Required CI gate.

- 2026-09-10, Phase 4a: Restored the active Codex High and Ultra bindings to immutable contract 2 (Astra Max and Astra Ultra). Established contract 1 Sol sessions and Works retain their exact routes, and source-contract fences continue to refuse mixed-version fresh effects. Fresh managed-provider imports now select the active binding; existing imports retain their stored binding. CLI help, browser fixtures, public preset copy and the shadow-routing reference follow the new binding. Routing remains shadow-only; Phase 4b must separately add start-time decisions and the disable setting without reinterpreting historical requests.

- 2026-09-10, Phase 4a validation: The rebased daemon service, provider-switch, Work service and local-transport suites passed 769 tests. The storage run passed 764 cases outside WorkStore; one stale Work assertion still assumed an active contract 1 Work, so its frozen Devin guard control now uses an explicit reduced historical contract 1 fixture, and all 127 WorkStore cases pass. App command contracts passed 57 tests, current cloud payload and registry/start contracts passed 40 and 36 tests, public HTML/Markdown content passed 52 tests, compiled app and runtime-style boundaries passed 78 tests, package-policy tests passed 11 tests, and CI command/shard equivalence passed 33 tests. Installer pins, renamed repository adoption, exact packed inventory, production-source policy, focused lint and diff checks passed. The renderer admission retains exact raw bytes and an unchanged React DOM dependency digest; its new allocation differs from reviewed fixtures only by a consistent identifier mapping. Complete Required CI remains the integration gate, and start-time routing remains unimplemented.

- 2026-09-10: Continued the interrupted workstream from its recorded session. Phases 1 to 3 are merged in #178. Desktop removal (#179), the usage view (#181), Astra binding, start-time routing, and documentation acceptance remain open. The documentation branch moves installation references to Start and project/license details to Status; the overview stays a guide index, and browser instructions now describe the shipped inline cards and machine-only composer. No release or runtime activation is implied by these source changes.
- 2026-09-10: Phase 6 daily-history slice repaired after independent review. The app reads/decrypts the unchanged v1 daily archive and retains unreadable newest rows as unknown. It labels reports as historical, exposes absolute scheduled reset instants, and never converts stale or missing values into full capacity. Current usage, Claude observations, credits and forecasts remain unavailable. The legacy producer/parser/bounds and retention stay unchanged; the current-head acceptance criteria above remain pending.

- 2026-09-10: Phase 4b architecture amended after tracing the command boundary: only browser starts have a prompt to classify. The browser selects an already-supported, exact-contract preset before its ordinary command is recorded. The earlier proposal for an active shadow router and `oompa settings routing off` is replaced by a browser-only Automatic effort preference. This preserves explicit callers, the unchanged CLI idle-start contract and shadow-study gates. Default-on absence is distinct from unreadable or malformed preference storage, which fails off; only a finite non-sensitive on/off flag is persisted. No historical routing rule is invented.
- 2026-09-10: Phase 4b focused evidence: policy/storage/builder tests 35/0; app suite 623/0 including its owned production build; classifier, browser source, import policy and release-workflow tests 132/0; typecheck and changed-source lint passed; managed baseline current. The new React DOM resource function fingerprint `38c65e36bdaa72af11d634e191f31be3f940b40a55f8f1061fa0205cef460ad2` matches all six reviewed fixture token structures under consistent identifier bijections, with unchanged pinned renderer source. The compiled-browser gate now checks effort hints and persisted disable/re-enable, including actual Max selection when converged with Astra contract 2. Browser and final CI execution remain owned by integration.

- 2026-09-10, converged candidate: Desktop removal, Astra binding, automatic browser effort, daily usage history and documentation are joined on `codex/oompa-app-convergence-20260910`. Independent review covered the complete source and joins. It caught the legacy producer's missing-window `unlimited` bit and silent limit truncation; actual-producer regressions now keep both unknown. Focused repair passed 21 tests with 486 assertions. The combined app run passed 655 tests and exposed only the new renderer allocation; exact source review and the repaired compiled-boundary suite passed 94 tests. Combined site/cloud/browser-contract/CI-equivalence checks passed 191 tests, installer and marketing checks passed 54, and typecheck, lint, adoption and diff checks passed. The coupled provider-switch setup now uses the existing tested current-schema template for two fixtures only; 15 focused cases pass without deadline or assertion changes. The exact release inventory is 215 entries, 10,761 canonical bytes, digest `a73a65fe24a58176bbd11341e08e5fc2c269f7a7b15eaff99eb44fac67bbb5b7`; installer pins are consistent with the prepared v0.8.0 tag. Converged compiled-browser acceptance and fresh Required CI remain pending. No live usage, release admission or runtime activation is claimed.

- 2026-09-10, converged acceptance repair: The compiled browser driver now checks the moved first-account and first-session fragments at their Start guide destination. Full local browser acceptance passed with 204 evidence rows and all native custody cases. Two current-writer storage fixtures now expect Astra while archived Sol fixtures remain unchanged; 52 focused cases and 1,392 assertions passed. The independently authored README and candidate notes now carry the prepared installer's exact runtime digest; 40 package-content and release-workflow cases passed. Five independent lost-start refusal checks were separated into individual cases after the combined case exceeded its five-second deadline; all assertions and deadlines remain, and 17 focused lifecycle and lost-start cases passed. Independent review found no blocker in these repairs. Fresh Required CI remains the final source gate; artifact publication and runtime activation are not claimed.

- 2026-09-11, CI fixture preparation: The coupled provider-switch seed fixtures now use separate five-second setup hooks under one existing owner; their combined recovery assertions retain the five-second test deadline. The complete provider-switch file passed 116 tests and 5,690 assertions, and its previously failing macOS CI shard passed. A separate macOS CI timeout in the direct-applied Claude start-scope proof is addressed by preparing its real database in an owned five-second setup hook. The before/after process-release proof retains all nine assertions and its five-second deadline. Existing cancellation helpers join pending work before closing storage; the full readback file passed 59 tests and 201 assertions, with typecheck and affected lint passing. Fresh Required CI remains pending. These test-only repairs do not change provider behavior, admit a release, or clear the hosted capacity hold.

- 2026-09-11, memory fixture preparation: Combined CI exposed a five-second Ubuntu timeout in the coupled project-memory integration proof. A bounded local diagnostic passed all eleven assertions and measured most case time in fresh real database preparation. That unchanged preparation now has an owned five-second setup hook; the complete remember, query, explain, share and conflict proof retains its five-second test deadline. Setup and proof return observed tasks that teardown joins before closing storage. The full memory-coordinator file passed 39 tests and 242 assertions, and affected lint passed. No schema, runtime, retry or operational gate changes are included. Fresh complete Required CI remains pending.

- 2026-09-11, native closure fixture: Combined Ubuntu CI observed a successful native child exit without complete output-pipe closure. Six successful close and cleanup assertions now await the native promise before matching its unchanged result, avoiding the pinned Bun promise matcher's event-loop re-entry. Independent source review, portable Darwin fixture validation, typecheck and affected lint passed; the previously failing Linux native case still requires fresh CI evidence. Native code, deadlines, natural EOF, scope, rejection and cleanup requirements remain unchanged.

- 2026-09-11, storage fixture preparation: Three unchanged Ubuntu storage contracts exceeded their five-second deadlines. Synthetic adoption-v36 construction and adversarial queue-schema DDL now commit in bounded fixture transactions, with foreign keys enabled before the synthetic transaction. The tested real first migration and readonly/writable refusal opens still run after fixture commit. All four affected contracts preserve their original assertions and default five-second deadlines, including frozen fixture, foreign-key, integrity, unchanged-state and authority checks. The focused comparison passed 4 tests and 12,247 assertions before and after the repair; total local duration changed from 6.87 to 5.66 seconds with individual-case variation. Runtime storage and schema code are unchanged by this fixture repair. Fresh Ubuntu CI must confirm the timing result.

## Remaining current-usage dependencies

The following units complete the existing Phase 8 contract; this PR does not implement or activate them. Keep daily archive admission and retention unchanged. Each source or hosted unit requires its focused regression evidence and independent review before the producer becomes reachable.

| Unit | Exact source boundary | Dependency and bounded acceptance |
| --- | --- | --- |
| Coherent local facts | `src/storage/state-store.ts`, `src/storage/provider-usage-source-metadata.test.ts`, `src/domain/provider-usage.ts`, `src/cloud/daemon-adapters.ts` | Extend the existing read-only metadata snapshot with independently validated newest Codex quota and Claude quota/accounting, current identity and reset evidence in one bounded synchronous read. Preserve per-component clocks, missing sidecars, independent provider authority and explicit unavailable states. No refresh or provider effects. |
| Head projection | `src/cloud/usage-components-v2.ts`, `usage-display-v2.ts`, `usage-head-v2.ts`, `usage-context-v2.ts`, `usage-envelope-v2.ts`, plus a new pure producer module | Consume only the coherent facts. Use the existing provider/device/source context and optional Codex account-match link. Leave unsupported advice unavailable. Prove maximum bounds, source joins, key identity, tamper and no v1 fallback. Claude accounting is not subscription throughput. |
| Hosted current-head authority | New `convex/usageHeads.ts`; `convex/schema.ts`, `quota.ts`, `lifecyclePolicy.ts`, `maintenance.ts`, account-deletion/device-revocation handlers and tests; `src/cloud/client.ts`, `contracts.ts` | Separate current heads from daily snapshots. Admit monotonic revisions and exact-ciphertext replay, a 60-second coalescing cadence, finite write budgets, pagination and explicit incomplete state. Account for head, replay and retirement bytes/records in every exhaustive lifecycle map. Test lost-response reconciliation after later advancement, retirement, revocation and deletion. |
| Durable producer and replacement | `src/cloud/daemon-journal.ts`, `daemon-bridge.ts`, `daemon-adapters.ts`, `local-control.ts`; `src/cloud/usage-key-v2.ts` | Join hosted authority and coherent projection through an exact-byte write-ahead outbox, persistent encryption high-water limit, source revisions and key-change retirement/recovery. Do not re-encrypt pending requests or reuse an old key's cursor. Complete crash/restart and source-removal tests before runtime wiring. |
| Browser mapping and current view | `src/cloud/payloads.ts` companion envelope/mapping; `app/src/data/functions.ts`, `registry.ts`, `usage.ts`; `app/src/model/usage-meter.ts`; usage components and browser fixture | Join paginated provider/device/source heads under independently selected expected context. Deduplicate Codex only through its proved account-match link; never pool Claude bindings as subscriptions. Show component-specific freshness and last-turn semantics. Add trustworthy source-specific velocity before restoring tokens/minute, runway or throughput charts; dual-read v1 as history only. |

Full Phase 6 acceptance still requires these dependencies, meaningful runway/property and live-cadence tests, compiled browser checks, fresh Required CI, and the separate hosted deployment/production readback gates when deployment is in scope.

- 2026-09-11, Phase 6 source composition preparation: The coherent private facts reader and a standalone native V2 projector now compose cached source facts into the existing closed head codec. Quota, last-result accounting, readiness, order, policy and reset recovery remain independent; original component clocks and nullable credits are preserved. Source IDs must match independently supplied context, and Codex grouping retains the existing normalization and HMAC preimage. A damaged newest component remains unknown; a private code outside the public grammar refuses its complete component without truncating rows. A reset policy bound to another identity remains unavailable in the public current-identity block, while independently retained recovery stays visible and blocks advice. No policy evaluation, revision allocation, runtime caller, encryption writer, hosted mutation or browser integration is added. Earlier preparation validation passed: facts, metadata and actual-reader composition 101 tests / 3,385 assertions; existing V1/V2 codecs and provider projection 87 tests / 31,185 assertions; release command/shard coverage, import-cycle, package-content, package-policy and installer/security tests 63 tests / 1,444 assertions. Typecheck, affected lint, security primitives, installer pins and exact packed public-source/inventory admission passed. The reviewed package inventory contains 216 entries and 10,814 canonical bytes. The final independently reviewed direct-input guard also maps native V1 zero credits to unknown. Its focused replay passed all 25 composition cases / 1,389 assertions, with fresh typecheck, affected lint and admission of the actual final package archive against that inventory. Independent source review is clear; the exact integration Required gate remains necessary; current-head production and full live-meter acceptance remain incomplete, with the capacity hold unchanged.

- 2026-09-11, joined archive inventory: The actual archive joining the shared theme and reviewed read-only usage source contains 216 entries, including 208 files and eight directories, with 10,814 canonical inventory bytes and digest `4c86088bd07332bdeb0d6677b0f55d03bb8a80d351a85e8cbe6b5f305993dadd`. Every archived file matches its joined source blob. Compared with the separately reviewed usage archive inventory, only the package manifest size changes, from 7,125 to 7,126 bytes, because of the longer shared UI version. The strict count, modes, sizes and digest gate remains intact. Final complete package validation and Required CI remain pending; no release or runtime activation is claimed.
